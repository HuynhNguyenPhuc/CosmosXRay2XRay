# === Core Training ===
lightning
hydra-core==1.3.2
omegaconf==2.3.0

# transformers==4.51.3 (pulled in by the cosmos-predict2.5/cosmos-transfer2.5 submodules'
# cosmos-oss dependency) hard-requires huggingface-hub<1.0 at import time; diffusers/gradio/
# accelerate/peft/timm resolve to huggingface-hub>=1.0 otherwise, which breaks
# `import predict2_5` / `import transfer2_5` with
# "ImportError: huggingface-hub>=0.30.0,<1.0 is required ... but found huggingface-hub==1.x".
huggingface-hub<1.0,>=0.30.0

# === Medical Imaging & DiffDRR ===
monai==1.5.1
nibabel  # NIfTI file reader for MONAI
diffdrr
torchio
scikit-image

# === Video & Image Processing ===
decord==0.6.0
tqdm==4.66.5
imageio[pyav,ffmpeg]==2.37.0

# === General Dependencies ===
einops==0.8.1
gdown
rich
click

# === Logging & Visualization ===
wandb
tensorboard>=2.20.0
tensorboardX
setuptools==81.0.0
gradio

# === Testing ===
pytest

# === Cloud Storage ===
google-cloud-storage